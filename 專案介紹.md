# Cale's Breathe 熱蠟美肌預約系統（v2 / cales-breathe-v2）

> **用途**：面試口試與作品說明用；技術細節以儲存庫內規格書為準。

#### Demo（請依實際部署更新）

- **正式環境**：前端 **Vercel**；後端 **Google Cloud Run**；資料庫 **Supabase**（PostgreSQL）。API 經由前端網域的 `/api/*` 轉發至 Cloud Run（見 [docs/本地與雲端部署.md](./docs/本地與雲端部署.md)、[`cales-breathe-v2-vite/vercel.json`](./cales-breathe-v2-vite/vercel.json)）。
- **本機整合測試（Docker Compose）**：`http://localhost`（或 `.env` 的 `GATEWAY_HTTP_PORT`）— Postgres + API + NGINX gateway，見根目錄 [README.md](./README.md)。
- **公開網址／影片 Demo**：`_在此填入 Vercel 網域等_`

#### 測試帳號（若有）

- `_帳號 / 密碼或 OAuth 說明_`

---

一個為熱蠟工作室設計的**預約與會員平台**：前台瀏覽服務與線上預約、會員登入與訂單管理；後端以 **FastAPI** 實作 REST API、預約商業規則、並可整合 **LINE Messaging** 與 **Google Calendar**。本版為 **v2 重構**（Monorepo：API + Vite 前端；**本機**以 Docker Compose 內 **NGINX gateway** 整合靜態與 `/api/`），目標為環境一致、易於面試展示與後續上線。

**與舊版差異（面試可一句帶過）**：舊版為 Node（Express）+ Vercel／Render 等組合；v2 後端改為 **Python / FastAPI**，正式環境為 **Vercel（前端）＋ Cloud Run（API）＋ Supabase（PostgreSQL）**；**本機**則以 **Docker Compose** 模擬完整站（Postgres + API + NGINX 閘道）。曾評估以 **GCP VM** 自架與 compose 類似架構，**已取消**。線上瀏覽器透過 Vercel 的 `/api` 轉發與後端同網域策略，利於 Cookie／OAuth。

---

## 功能特色

### 首頁與品牌呈現

- 形象內容、店家介紹、顧客回饋等（React 頁面）

### 服務項目

- 服務介紹、價目與套餐呈現、導向預約流程

### 線上預約

- 多步驟預約（選擇服務組合、時段等）
- 後端支援**多服務一筆預約**、**分類互斥**（同一 category 至多選一項）、**30 分鐘時間格線**與**時段衝突檢查**（單人工作室、全域互斥）

### 會員與身分

- 註冊／登入（含 **Google、LINE OAuth** 等流程，實際啟用依環境變數與後端設定）
- 個人預約／訂單查詢、帳戶資料維護（與 Legacy API 相容之路由並存於後端時，可一併說明遷移策略）

### 業主／營運視角（API 層面）

- 業主查詢訂單、代客相關流程（權限與 `owner` 角色—面試可對照 [cales-breathe-v2-api/docs/SPECIFICATION.md](./cales-breathe-v2-api/docs/SPECIFICATION.md)）

### 整合與自動化（展示亮點）

- **LINE Messaging Webhook**：接收事件、驗簽（公網 HTTPS／ngrok 本機測試）— 見根目錄 README
- **Google Calendar**：預約與行事曆事件同步（建立／更新／取消依實作進度說明）

### 響應式與體驗

- 支援手機與桌機瀏覽（Tailwind 等）

> **截圖／GIF**：若需與舊版履歷同款視覺，可於 `cales-breathe-v2-vite` 錄製後將圖檔放在 `docs/assets/` 並在此以相對路徑引用。

---

## 技術棧

| 層級 | 技術 |
| --- | --- |
| 前端 | React、Vite、Tailwind CSS、React Router |
| 後端 | **Python、FastAPI、Uvicorn**、Pydantic、**SQLAlchemy 2.x** |
| 資料庫 | **Supabase（PostgreSQL）**（正式）；本機 Docker Compose 內建 **Postgres** 容器；單檔開發可 **SQLite**（見 API `database` 設定） |
| 認證與安全 | OAuth 2.0（Google／LINE）、JWT／Cookie 與 CORS／SameSite 等設定（依 `.env.example`） |
| API | RESTful API、OpenAPI（Swagger `/docs`） |
| 部署與基礎設施 | **正式**：**Vercel**、**Google Cloud Run**、**Supabase**。**本機整合測試**：**Docker Compose**、**NGINX**（`deploy/`：靜態站 + `/api/` 反代至後端，與線上「前端 + API 路徑」概念對齊） |
| 版本控制 | Git |

---

## 面試口述可強調的後端要點

1. **領域規則寫在後端**：預約衝突、分類互斥、取消時間窗與角色權限，避免只依前端驗證。
2. **契約清楚**：HTTP 狀態碼（例如 400／403／409）與錯誤訊息可測試、可對齊規格書。
3. **第三方整合**：Webhook 驗簽、OAuth callback 與公開網址一致性（`API_PUBLIC_BASE_URL`）。
4. **可演進**：Alembic 遷移（本機／Supabase）、pytest／CI 等階段規劃見 [SPECIFICATION.md](./cales-breathe-v2-api/docs/SPECIFICATION.md)；本機與雲端 DB 皆為 PostgreSQL 時路徑一致。

---

## 未來計畫（與規格書一致者可並行追蹤）

- 業者後台：月曆視圖、預約編修、客戶名單
- 預約提醒（LINE／Email）
- 資料庫遷移（Alembic）、自動化測試與 CI
- CI/CD 與測試覆蓋率（見 [SPEC_INTERVIEW_V1.md](./cales-breathe-v2-api/docs/SPEC_INTERVIEW_V1.md)；文件中若仍提及 Render，以目前 **Cloud Run + Vercel** 為準）

---

## 相關文件（儲存庫內）

- [README.md](./README.md) — 本機 Docker Compose 啟動與環境變數
- [docs/本地與雲端部署.md](./docs/本地與雲端部署.md) — 正式環境（Vercel、Cloud Run、Supabase）總覽
- [cales-breathe-v2-api/docs/SPECIFICATION.md](./cales-breathe-v2-api/docs/SPECIFICATION.md) — API 與驗收現況
- [cales-breathe-v2-api/docs/SPEC_INTERVIEW_V1.md](./cales-breathe-v2-api/docs/SPEC_INTERVIEW_V1.md) — 面試目標與敘事
- [CALES-BREATHE(舊版).md](./CALES-BREATHE(舊版).md) — 前代技術棧與 Demo 連結對照

---

## 作者

### Ray Lu

rayfong61@gmail.com

---

## License

本專案為學習用途，未授權商業使用。如需合作請來信洽談。
